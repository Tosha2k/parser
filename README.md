## parser example
### Use symfony and:
+ DomCrawler
+ Cache
+ Console
+ Doctrine ORM
+ Twig

### install and use
+ clone
+ composer install
+ bin/console doctrine:migrations:migrate
+ bin/console doctrine:fixtures:load
+ bin/console app:pars
